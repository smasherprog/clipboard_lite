image: Visual Studio 2017

configuration:
  - Relase 
  
clone_folder: c:\projects\sc

platform:
- x86
- x64

artifacts:
  - path: '$(CONFIGURATION)\**\*.dll'
    name: clipboard_lite_artifact
    
install:
  - ps: If ($env:Platform -Match "x86"){ $env:CMAKE_ARCH="" } Else { $env:CMAKE_ARCH=" Win64"}
  - cd c:\projects\sc
  - mkdir build
  - cd build
  - cmake ..

build_script: 
  - cmake -DBUILD_SHARED_LIBS=ON . 
  - echo cmake --build . --config %CONFIGURATION% --target ALL_BUILD 
  - cmake --build . --config %CONFIGURATION% --target ALL_BUILD 
  - cmake -DBUILD_SHARED_LIBS=OFF .
  - cmake --build . --config %CONFIGURATION% --target ALL_BUILD 
  
deploy:
  provider: GitHub
  auth_token:
    secure: WCGi6SXwa6VaPyLH6fZCrmTix+e2FA3zwzhNg8dSf3EdrcecGIR3Z0qG/vssOdfA
  artifact: clipboard_lite_artifact
  draft: false
  prerelease: false
  on:
    branch: master                 # release from master branch only
    appveyor_repo_tag: true 
    